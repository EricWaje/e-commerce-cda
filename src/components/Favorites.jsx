/* import React from 'react'

const Favorites = () => {
    //{fav} -> Contexto
  return (
    {
        fav.map((fulanito)=>(
            <Card prod={fulanito} key={fulanito.id}/>
        ))
    }
  )
}

export default Favorites */
